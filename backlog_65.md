# Фаза 6.5 — Фасадный спринт (backlog)

**Цель спринта:** первое впечатление соларпанк-утопии 2480. Сейчас на экране: ровный пад 18×18 с грид-линиями, янтарный пятиугольник-ровер со следом, HUD-текст, тёплый DirectionalLight2D. Работает — «но только это и всё». К концу спринта: живое небо, местность из seed, стартовое поселение, ровер 2.0 и пульс Ноосферы на HUD. Ядро и тестовая дисциплина не страдают.

**Текущее состояние (проверено по коду):** MVP (фазы 0–6) закрыт, `scene_integration.gd` / `boot_check.sh` / `smoke.gd` / `mvp_smoke.gd` зелёные. Визуал: `world.tscn` (WarmLight + Grid + Camera), `grid.gd` рисует пад + грид через `_draw`, `rover.tscn` — Hull/Beacon Polygon2D + Trail Line2D, `rover_visual.gd` — поворот/сквош/след. Seed читается из GDScript как `sim.get("seed")` (так делает `hud.gd`).

---

## Зафиксированные рамки спринта (не пересматривать)

- Всё в 2D top-down. Camera2D / Node2D, конверсия f64→f32 с Y-флипом (`crates/neogen-gdext/src/coords.rs`) — не трогать.
- Без аудио. Без 3D. Без новых крейтов в `neogen-core`. Ядро только ЧИТАЕМ: seed, тик, позиции. Визуал — только Godot-сторона.
- Земля генерируется **из seed ядра**, детерминированно. Раскладка задач 7.x (тайлы в core, golden на карту тайлов) не предвосхищается: фасадный генератор — временный слой, сносится при фазе 7.
- Стиль: соларпанк (тёплый свет, зелень, стекло, clean lines), сдержанно, читаемость прежде всего. Никаких внешних ассетов — только полигоны/шейдеры/программные текстуры Godot.
- Тестовая дисциплина: UI-bound проверки — poll-until-timeout; `scene_integration.gd` (реальная main.tscn) и `boot_check.sh` остаются зелёными; smoke не деградирует; каждая визуальная задача добавляет свой пункт ручного чек-листа в README.

## Ключевое решение: путь генерации земли (проверено, не гипотеза)

**Выбрано: детерминированный дублирующий алгоритм в GDScript** от `sim.get("seed")`, а не доступ к RNG ядра:
- В мосте (`sim_node.rs`) наружу торчат только seed (`#[var]`), тик, позиции/скорости роверов и скриптовое API — тайлов в core нет до фазы 7, и делать их ради визуала значит украсть дизайн 7.1 и тронуть golden-детерминизм. Это запрещено рамками.
- В GDScript генерация строится как **чистая функция ячейки**: `tile(seed, x, y)` / `props(seed, x, y)` через целочисленный хеш (FNV-1a-подобный над 64-битными int) — порядок обхода не важен, чанки не нужны, тривиально тестировать. Godot-`RandomNumberGenerator` (PCG32, детерминирован при фиксированном seed) допустим как вспомогательный, но база — чистый хеш от (seed, координаты).
- Генерация живёт в **отдельном pure-скрипте** (`ground_gen.gd`, только static-функции, без сцены) — headless-тест детерминизма гоняется без бута сцены.
- ASSUMPTION для worker (дёшево проверить в задаче 1): GDScript-инт 64-битный, переполнение при умножениях хеша оборачивается молча — убедиться тестом «два прогона одного seed → идентичный хеш» (он и так обязателен).

---

## Задачи

### ☑ 6.5.1. Фасадный milestone: небо + земля + ровер (минимальный сквозной апгрейд) — M, риск: средний ✅ `bdd6aed`

Первое впечатление появляется рано: одним заходом меняем фон, землю и ровер минимально, но по всем трём направлениям сразу.

- **Файлы:** `godot/shaders/sky_gradient.gdshader` (новый), `godot/scripts/sky.gd` (новый, CanvasLayer), `godot/scripts/ground_gen.gd` (новый, pure static), `godot/scripts/ground.gd` (новый, Node2D-отрисовка), `godot/scenes/world.tscn` (+Ground), `godot/scenes/main.tscn` (+Sky под миром), `godot/scenes/rover.tscn` (второй слой корпуса), `godot/tests/facade_determinism.gd` (новый headless), README (раздел «6.5 Фасад»), `.github/workflows/ci.yml` (добавить тест в godot-джобу).
- **Шаги:**
  1. Небо: полноэкранный градиент-шейдер (тёплый рассвет: янтарный горизонт → приглушённый бирюзовый зенит), CanvasLayer под миром (`layer` ниже world, follow viewport). Без анимации — статика пока.
  2. Земля: `ground_gen.gd` — pure-хеш `(seed, x, y) → вид ячейки` (4 базовых вида: мох светлый / мох тёмный / земля / редкий камень), стартовый пад из `START_PAD_HALF_SIZE` остаётся поверх (синк с константой ядра уже задокументирован в `grid.gd`). `ground.gd` рисует область вокруг старта одним `_draw`-проходом (например, 64×64 ячеек, запас за границы зума). Грид-линии — оставить, ослабить.
  3. Seed-источник: узлы читают `sim.get("seed")` по образцу `hud.gd` (explicit-path, warn при отсутствии Sim — паттерн silent-by-design уже принят в проекте).
  4. Ровер: минимальный второй слой — кабина (стеклянный полигон с бликом) + антенна (линия с точкой-огоньком). Корпус и Beacon не трогать.
  5. Тест `facade_determinism.gd`: без сцены — два прогона `ground_gen` на seed 42 → идентичный хеш раскладки; seed 43 → хеш другой.
- **DoD:**
  - [ ] `cd godot && godot --headless --script res://tests/facade_determinism.gd` → `FACADE DETERMINISM OK`, код выхода 0.
  - [ ] `bash godot/tests/boot_check.sh`, `smoke.gd`, `scene_integration.gd` — зелёные; CI godot-джоба зелёная с новым тестом.
  - [ ] Ручной чек-лист README «6.5.1 — что увидеть глазами»: ① фон — градиентное небо, а не серый клир; ② земля вокруг пада неоднородная (минимум 3 оттенка/вида), пад читается как площадка; ③ ровер — двухслойный (корпус + кабина/антенна), по-прежнему янтарный и направленный; ④ грид-линии не спорят с землёй; ⑤ FPS ≥ 60.
- Зависит от: —

### ☑ 6.5.2. Атмосфера: виньетка, пыльца, мягкость света — S, риск: низкий ✅ `b165ec8`

- **Файлы:** `godot/shaders/vignette.gdshader` (новый), `godot/scripts/atmosphere.gd` (новый: GPUParticles2D пыльцы/искр вокруг камеры, мировой слой), `godot/scenes/world.tscn` (твики WarmLight: energy/shadow_blur, +Atmosphere), `godot/scenes/main.tscn` (+CanvasLayer с виньеткой поверх), README (чек-лист 6.5.2).
- **Шаги:** виньетка — лёгкое затемнение краёв (не мешает читаемости HUD и консоли); частицы — 2–3 дюжины медленно дрейфующих тёплых точек в зоне видимости, respawn за кадром камеры; свет — смягчить (ниже energy, шире blur), проверить, что тени ровера/структур остались читаемыми.
- **DoD:**
  - [ ] `boot_check.sh` и `scene_integration.gd` зелёные (headless с частицами не падает).
  - [ ] Ручной чек-лист «6.5.2 — что увидеть глазами»: ① по краям кадра мягкое затемнение; ② в воздухе дрейфуют редкие пылинки/искры; ③ свет мягче, чем в 6.5.1, тени живые; ④ HUD/консоль/редактор читаются без борьбы с виньеткой; ⑤ FPS ≥ 60.
- Зависит от: 1

### ☑ 6.5.3. Земля 2.0: трава-спирали, скалы, кристаллы, разметка пада — M, риск: средний ✅ `4445910`

- **Файлы:** `godot/scripts/ground_gen.gd` (расширение: ячейка → декор: спираль папоротника/пучок, скала, кристалл; частоты через хеш, редкость кристаллов явной константой), `godot/scripts/ground.gd` (отрисовка декора полигонами; кристалл — лёгкое свечение, без света-источника), `godot/tests/facade_determinism.gd` (хеш включает декор), README (чек-лист 6.5.3).
- **Шаги:** декор рисуется поверх базовой земли; скалы/кристаллы пишут себя в структуру занятости (нужна задаче 4 для расстановки поселения); стартовый пад — рамка/уголки-разметка, читается как посадочная площадка ровера; плотность декора сдержанная (читаемость прежде всего).
- **DoD:**
  - [ ] `facade_determinism.gd` зелёный (раскладка с декором стабильна между прогонами, seed 43 отличается).
  - [ ] `boot_check.sh`, `smoke.gd`, `scene_integration.gd` зелёные.
  - [ ] Ручной чек-лист «6.5.3 — что увидеть глазами»: ① местность узнаваемая: мох + спирали травы + редкие скалы; ② в кадре при старте виден минимум 1 кристалл со свечением; ③ стартовый пад читается как размеченная площадка; ④ декор не мешает видеть ровер и HUD; ⑤ FPS ≥ 60.
- Зависит от: 1

### ☑ 6.5.4. Стартовое поселение: купол, солнечные панели, мачта — M, риск: средний ✅ `85904b6`

- **Файлы:** `godot/scripts/settlement.gd` (новый: детерминированная расстановка из seed вокруг пада, читает занятость из ground_gen), `godot/scenes/settlement.tscn` (новый: структуры полигонами — стеклянный купол с бликом, 2–3 наклонные солнечные панели, мачта с огоньком; LightOccluder2D на структурах для теней), `godot/scenes/main.tscn` (+Settlement), `godot/tests/facade_determinism.gd` (хеш расстановки), README (чек-лист 6.5.4).
- **Шаги:** 3–5 структур, статичные; позиции — чистая функция (seed, занятые ячейки), с отступом от пада так, чтобы не перекрывать старт ровера и HUD-зону; тени от тёплого DirectionalLight2D (shadow_enabled уже включён).
  - ASSUMPTION (проверить глазами в первую очередь): LightOccluder2D + Polygon2D дают мягкие тени от текущего WarmLight; если тени не читаются — усилить контраст земли у подножия структур (клемма-тень полигоном), не трогая настройки ядра света.
- **DoD:**
  - [ ] `facade_determinism.gd` зелёный (два прогона seed 42 → идентичная расстановка).
  - [ ] `boot_check.sh`, `smoke.gd`, `scene_integration.gd` зелёные.
  - [ ] Ручной чек-лист «6.5.4 — что увидеть глазами»: ① при старте видно поселение: купол + панели + мачта (3–5 структур); ② тени структур падают согласованно со светом; ③ ровер стартует на паде, ничего не перекрыто; ④ панели/купол — соларпанк-палитра (тёплые стёкла, чистые линии), без визуального шума; ⑤ FPS ≥ 60.
- Зависит от: 3

### ☐ 6.5.5. Ровер 2.0 финал: колёса/парение, след со свечением — S, риск: низкий

- **Файлы:** `godot/scenes/rover.tscn` (колёса/юбка парения полигонами), `godot/scripts/rover_visual.gd` (анимация от effective_speed: вращение колёс или осцилляция парения; след — width-кривая + шиммер/свечение вторым Line2D), README (чек-лист 6.5.5).
- **Шаги:** механизм выбранный — «парение»: ровер чуть «дышит» на подушке на стоянке (низкоамплитудная синусоида Body), на ходу — наклон уже есть из 4.2, добавить вращение/мерцание колёсных полигонов от скорости; след: тёплый градиент + мягкое свечение (второй более широкий полупрозрачный Line2D под основным), затухание как сейчас. Существующие smoke-чеки зеркала и движения не ломать (интерфейс Body/Trail не менять).
- **DoD:**
  - [ ] `smoke.gd`, `mvp_smoke.gd`, `scene_integration.gd` зелёные.
  - [ ] Ручной чек-лист «6.5.5 — что увидеть глазами»: ① на ходу колёса/опоры читаются в движении; ② на стоянке ровер слегка «дышит»; ③ след — тёплое свечение с плавным затуханием, не «полоса-линейка»; ④ сквош и доворот курса из 4.2 сохранились; ⑤ FPS ≥ 60.
- Зависит от: 1 (можно параллельно с 2–4)

### ☐ 6.5.6. Пульс Ноосферы: индексы энергия/экология на HUD (прототип) — M, риск: средний

- **Файлы:** `godot/ui/hud.tscn` (две компактные шкалы), `godot/scripts/hud.gd` (модель значений + события), `godot/scripts/noosphere.gd` (новый: pure-формулы значений от событий, отдельным скриптом для тестируемости), `godot/tests/smoke.gd` (poll-чек на изменение значений), README (чек-лист 6.5.6 + пометка «прототип»), `docs/MVP_CHECKLIST.md` не трогать.
- **Шаги:** источник событий — только то, что видно Godot-стороне без изменения ядра: скорость/перемещение ровера (`get_rover_speed/position`), attach/stop скрипта. Черновая формула: движение → энергия медленно убывает, стоянка → солнечная подзарядка растёт (мы у солнечных панелей!), экология — плавный отклик на суммарную активность, clamp 0..100. Шкалы — соларпанк-стиль (тёплые градиенты), рядом с панелью статуса. Проверки — poll-until-timeout (UI-bound, правило из FIX-раундов).
  - **Задокументировать в README у раздела:** значения — Godot-прототип до фазы 7; настоящие индексы (экология/доверие/гармония) переедут в core (задача 7.3), фасадные шкалы станут их зеркалом. Ядро, тики, детерминизм — не затронуты.
- **DoD:**
  - [ ] `smoke.gd` зелёный, включая новый poll-чек «после движения ровера значения индексов изменились».
  - [ ] `scene_integration.gd`, `boot_check.sh`, `mvp_smoke.gd` зелёные.
  - [ ] Ручной чек-лист «6.5.6 — что увидеть глазами»: ① на HUD видны две шкалы (энергия, экология); ② при движении ровера энергия visibly убывает, на стоянке восстанавливается; ③ экология реагирует плавно, без дёрганья; ④ прототипность задокументирована в README; ⑤ FPS ≥ 60.
- Зависит от: 1 (можно параллельно с 2–5)

### ☐ 6.5.7. Полировка впечатления: сквозной проход по чек-листу — S, риск: низкий

- **Файлы:** точечные правки 2–4 файлов визуала по итогам 6.5.2–6.5.6 (палитра, плотность, zoom-диапазоны), README (итоговый чек-лист спринта сводится в один раздел «6.5 — принять глазами»).
- **Шаги:** прогнать все ручные чек-листы 6.5.1–6.5.6 подряд на одном запуске; исправить только то, что ломает первое впечатление или читаемость (несогласованные оттенки, спорные плотности, блики, перекрытия UI). Новых сущностей не добавлять.
- **DoD:**
  - [ ] Все автоматические тесты зелёные (boot, smoke, scene_integration, facade_determinism, mvp_smoke).
  - [ ] Ручной прогон «5 минут глазами»: от старта до дальнего зума картинка читается как соларпанк-утопия; чек-лист «6.5 — принять глазами» в README пройден целиком; FPS ≥ 60 на ближнем и дальнем зуме.
- Зависит от: 2, 3, 4, 5, 6

### ☐ 6.5.8. Приёмочный прогон спринта + тег v0.1.0-mvp ⚠ — S, риск: низкий (тег — гейт пользователя)

- **Файлы:** без кода; приёмка по `docs/MVP_CHECKLIST.md` (A-автоматика, B-сквозной цикл, C-ручные фазы) + сводный чек-лист «6.5 — принять глазами» из README.
- **Шаги (полная лестница):**
  1. `cargo fmt --all --check` && `cargo clippy --workspace --all-targets -- -D warnings` && `cargo test --workspace`.
  2. `cargo build -p neogen-gdext --release` → артефакт в `godot/bin/`, `godot --headless --import`.
  3. `bash godot/tests/boot_check.sh` → `cd godot && godot --headless --script res://tests/smoke.gd` (SMOKE OK) → `res://tests/scene_integration.gd` → `res://tests/mvp_smoke.gd` (MVP SMOKE OK) → `res://tests/facade_determinism.gd`.
  4. Запуск `cd godot && godot` — ручные секции B чеклиста MVP + «6.5 — принять глазами».
  5. После явного подтверждения пользователя («принято») — тег `v0.1.0-mvp`. ⚠ **Тег ставит только prime и только после гейта пользователя — не автоматически** (правило из backlog.md, секция «MVP-приёмка»).
- **DoD:**
  - [ ] Все команды пп. 1–3 зелёные, код выхода 0 у каждой.
  - [ ] Ручные чек-листы B + «6.5 — принять глазами» закрыты.
  - [ ] Тег `v0.1.0-mvp` стоит (после подтверждения пользователя).
- Зависит от: 7

---

## Порядок и параллелизация

```
6.5.1 (фасадный milestone)
  ├─ 6.5.2 (атмосфера)      ─┐
  ├─ 6.5.5 (ровер 2.0)       ├─ могут идти параллельно после 1
  ├─ 6.5.6 (пульс Ноосферы) ─┘
  └─ 6.5.3 (земля 2.0) → 6.5.4 (поселение)
                 └────────────┴→ 6.5.7 (полировка) → 6.5.8 (приёмка ⚠)
```

- Один заход worker'а = одна задача. После каждой визуальной задачи — ревью.
- Спорные визуальные решения (плотность декора, палитра, характер частиц) worker фиксирует константами в шапке скрипта — правка полировки дешёвая.
- Конфликты файлов минимальны: 6.5.2/6.5.4/6.5.5/6.5.6 почти не пересекаются, 6.5.3 расширяет файлы задачи 1.
